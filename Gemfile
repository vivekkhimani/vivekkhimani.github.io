source "https://rubygems.org"

# Pins the exact Jekyll and plugin versions GitHub Pages builds with, so a local
# build matches what ships. Only needed for local preview; Pages ignores this.
gem "github-pages", group: :jekyll_plugins
gem "webrick"
