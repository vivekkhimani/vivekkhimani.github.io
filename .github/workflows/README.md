# Workflows

- `goodreads.yml` pulls the Goodreads shelves into `_data/books.json` once a day.
- `semgrep.yml` is the existing Semgrep scan.

The site itself needs no workflow. GitHub Pages builds the Jekyll source directly.
